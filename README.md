# Student-Management-System-CRUD-Create-Read-Update-Delete-operations-for-student-records
A simple C++ project that performs CRUD (Create, Read, Update, Delete) operations on student records using linked lists. It allows adding, viewing, updating, and deleting student data while demonstrating basic data structures and memory management concepts.
